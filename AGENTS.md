# AGENTS.md — Codex レビュー基準（共通テンプレート）

このファイルは Codex がレビュー時に読む。Claude の動き方は CLAUDE.md を参照。
仕様や方針が矛盾する場合、勝手に仕様を変えず指摘だけ上げること。
正式な仕様は GitHub Issue の受入条件を最優先とする。

## 前提
- 一人開発。シンプルさ優先。過剰な作り込みの指摘は不要
- 自宅PCと職場PCで開発を引き継ぐ（環境依存の前提を置きすぎない）

## レビューで重点的に見る
優先度の高い順:

1. データ整合性
   - DB（Firestore等）の読み書きでデータが壊れない／消えないか
   - 同じデータの二重登録・重複保存
   - 日付・数値・状態の境界（空・ゼロ・マイナス・月またぎ等）
2. 認証・データ分離
   - 未認証アクセスを弾けているか（security rules 等）
   - 自分以外のデータが読める/書ける経路がないか
3. 不具合・クラッシュ
   - 例外・null・非同期の競合で落ちないか
   - オフライン／通信エラー時の挙動
4. 個人情報・ログ
   - 個人情報・認証情報をコンソールや外部にログ出力していないか

## 見なくていい / 優先度を下げる
- 見た目・コードスタイルの好み
- 大規模アプリ向けの設計論（一人開発には過剰）

## 重大度の付け方
- P0: データ消失 / 未認証で他人のデータが見える / クラッシュで使えない
- P1: マージ前に必ず直すべき不具合
- P2: 直した方がいい
- P3: 将来の改善

## レビューの作法
- 推測だけで指摘しない。問題が起きる具体的な入力・経路・条件を書く
- 可能なら再現手順か再現テストを示す
- Claude の実装と意見が割れたら、勝った負けたで決めず再現テストで決着させる

## このアプリ固有の重点（必要に応じて追記）
- このアプリ特有の見どころがあればここに追記する
